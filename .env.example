# Copy this file to .env and fill in the values.
#
# All backend settings use the LTT_ prefix — see services/api-server/src/api/settings.py
# for the full list and docs.  Variables without LTT_ prefix are for Docker Compose only.
#
# More detailed templates:
#   .env.example.local  — local development
#   .env.example.dev    — deployed dev environment
#   .env.example.prod   — production

# ── Docker Compose (postgres container) ─────────────────────────────────────
POSTGRES_USER=ltt_user
POSTGRES_PASSWORD=ltt_password
POSTGRES_DB=ltt_dev
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# ── Backend ──────────────────────────────────────────────────────────────────
LTT_ENV=local
LTT_DATABASE_URL=postgresql+asyncpg://ltt_user:ltt_password@localhost:5432/ltt_dev
# LTT_CHECKPOINT_DATABASE_URL=postgresql://ltt_user:ltt_password@localhost:5432/ltt_dev_checkpoints
# LTT_REDIS_URL=redis://localhost:6379/0
LTT_AUTH_ENABLED=false
LTT_ANTHROPIC_API_KEY=your-api-key-here

# ── LangSmith (optional observability) ──────────────────────────────────────
# LANGSMITH_TRACING=false
# LANGSMITH_ENDPOINT=https://api.smith.langchain.com
# LANGSMITH_API_KEY=
# LANGSMITH_PROJECT=ltt-agent
